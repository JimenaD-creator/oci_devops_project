services:
  app:
    build: ./backend
    container_name: agileContainer
    ports:
      - "8080:8080"
    environment:
      - DB_URL=jdbc:oracle:thin:@managementdatabase_medium?TNS_ADMIN=/app/wallet
      - DB_USERNAME=MANAGER
      - DB_PASSWORD=Mgr##2024Oracle!
      - TELEGRAM_BOT_TOKEN=8597252989:AAESmYUpOa2ftnr5OuS0oQNKKKX96JXhKD8
    volumes:
      - ./backend/wallet:/app/wallet
    restart: unless-stopped