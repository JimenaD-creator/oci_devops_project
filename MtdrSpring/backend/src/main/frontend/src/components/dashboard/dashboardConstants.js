/** Max width for the overall dashboard content column. */
export const DASHBOARD_CONTENT_MAX_WIDTH = 1200;

/** Primary accent for dashboard progress / workload (non-red). */
export const DASHBOARD_PRIMARY_ACCENT = '#1565C0';
