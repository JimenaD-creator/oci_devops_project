# Copy to backend/.env and fill in real values (file is gitignored).
# Spring loads it on startup via DotEnvLoader + spring.config.import (repo root or backend/ cwd).
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-3.1-flash-lite
TELEGRAM_BOT_TOKEN=
TELEGRAM_BOT_USERNAME=
